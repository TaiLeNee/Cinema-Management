#ifndef COLOR_H
#define COLOR_H

#include <iostream>
#include <sstream>

using namespace std;


void green(const wstring& text);
void red(const wstring& text);
void yellow(const wstring& text) ;

#endif // COLOR_H